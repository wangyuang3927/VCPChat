# LocalShellExecutor 配置文件

# 返回模式: delta (仅返回增量输出) 或 full (返回完整终端内容)
SHELL_RETURN_MODE=delta

# 默认Shell优先级 (逗号分隔，按顺序检测)
SHELL_PRIORITY=bash,fish,zsh,

# 禁止执行的命令关键字 (逗号分隔)
FORBIDDEN_COMMANDS=rm -rf /,mkfs,dd if=

# 需要确认的命令关键字 (逗号分隔)
AUTH_REQUIRED_COMMANDS=sudo,su,chmod 777

# 命令执行超时时间 (毫秒)
COMMAND_TIMEOUT=60000